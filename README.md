# Diabetes_Analysis
This project uses the  Diabetes Dataset to build a machine learning model that predicts whether a patient has diabetes or not. The model is trained using Logistic Regression. After training, the model can predict the outcome (diabetic or non-diabetic) based on input medical features such as glucose level, BMI, age, etc.
